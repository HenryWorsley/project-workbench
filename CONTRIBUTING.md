# 参与贡献

感谢您改进 Project Workbench。提交代码前，请先阅读以下约定。

## 开发环境

- Node.js 22.13 或更高版本
- npm 10 或更高版本

```powershell
npm ci
npm run dev
```

提交前必须运行：

```powershell
npm run typecheck
npm run build
```

## 提交方式

1. 先创建 Issue，说明问题、目标和期望行为。
2. 每个 Pull Request 只解决一个清晰问题。
3. 在描述中列出测试方法、界面影响和数据兼容性影响。
4. 界面变化应附上截图；动态交互应附上真实录屏。
5. 不提交 `node_modules`、构建目录、输出视频或本地环境文件。
6. 新功能应同步更新 [功能与特色](docs/FEATURES.zh-CN.md)；安装和构建变化应同步更新 [各平台安装指南](docs/INSTALL.zh-CN.md)。

## 数据规则

- 只能提交虚构项目、虚构人员和模拟业务数据。
- 不得提交企业名、客户名、手机号、邮箱、地址、业务合同、生产计划或其他真实信息。
- 数据模型变更必须说明旧数据如何迁移和回滚。
- 涉及删除、覆盖或恢复数据的功能必须提供明确确认。

## 界面规则

- 延续当前视觉语言，避免随意增加颜色、阴影和弹窗。
- 动态效果不能妨碍文字阅读和鼠标操作。
- 尊重系统“减少动态效果”设置。
- 弹窗必须提供清晰的保存和关闭操作。
- 桌面常用分辨率和 Windows 缩放比例都应检查。
- 文档截图必须来自公开模拟数据版本，画面中不得出现真实企业或个人信息。

## Commit 建议

推荐使用简短、可读的前缀：

```text
feat: 增加任务依赖线
fix: 修复甘特图跨月拖动
docs: 更新 Windows 安装说明
chore: 更新构建依赖
```

提交贡献表示您确认所提交内容由您创作或有权按仓库最终采用的许可证发布。
