# 安全策略

## 支持范围

| 版本 | 安全更新 |
| --- | --- |
| `0.5.x` 测试版 | 支持关键安全问题修复 |
| `< 0.5` | 不支持 |

测试版不应作为关键业务数据的唯一存储位置。

## 报告安全问题

请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告入口。不要在公开 Issue 中发布以下内容：

- 可利用的漏洞细节或复现数据
- 真实个人、企业或客户信息
- Token、密钥、密码、证书或本地文件
- 可能导致数据删除、覆盖或泄露的完整攻击步骤

报告建议包括：受影响版本、操作系统、复现条件、影响范围和可行的缓解措施。维护者确认问题前，请不要公开披露。

## 项目安全边界

- 当前 Web 版默认不提供账号、云同步和业务服务器。
- 当前数据保存在浏览器本地空间，清理网站数据可能造成丢失。
- JSON 备份可能包含用户录入内容，分享前应自行检查和脱敏。
- 第三方依赖漏洞应同时提供依赖名称、版本和官方安全公告链接。

